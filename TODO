- cleanup interfaces
- release 1.0
- provide meta heuristic for treewidth
- parallelize exact_cutset?
- solvers as (callable/extensible) classes
- more treedecomposition structures
- make clean (e.g. grtd)
- use "using" instead of typedef for types that should not be exported

- dann PP+generic_search mit
  - initialem FI+TM als ub
  - initialem LBP(C)_deltaC
  - deltaC_least_c als lb
  - TM als refiner
- underlying backend gala sorted
- overlay muss noch ausprobiert werden
- (squashen vom overlay)
